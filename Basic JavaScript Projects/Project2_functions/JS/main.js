//a function that concatenates two strings and uses the += operator

function My_Function() {
    var sentence = "I am learning"; //variable sentence
    sentence += " a lot from this book!"; //+= operator
    document.getElementById("Concatenate").innerHTML = sentence; //display the concatenated string
}
